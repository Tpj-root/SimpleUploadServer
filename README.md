# SimpleUploadServer




No need to install anything.

Just run with:

```bash
python3 server.py
```

If you want to confirm your version:

```bash
python3 --version
```

Works in **Python 3.8 → 3.13+**.
